# Secrets Manager access is included in jakshwealth-infra/deploy/lambda/lambda_role_policy.json
# (attached as JW_<function>_<env>_LAMBDA_WRITE_ACCESS per Lambda module).
# No separate jakshwealth-lambda-secrets IAM policy is required.
