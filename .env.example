ENVIRONMENT=local
LOCAL_CONFIG_FILE=./config.local.json
# Set true to skip AWS Secrets Manager and use config.local.json only (offline dev).
CONFIG_SKIP_AWS=false
LOG_LEVEL=INFO
# Extra authorizer-context logging in jw_secure_data (auth/authz always log membership at INFO).
JW_AUTH_DEBUG=false
PORT=3000
